import { EmptyState } from '@/components/ui'

export default function AdminReportsPage() {
    return (
        <EmptyState
            eyebrow='Reports'
            title='Quest-wide summaries will be rendered from this shared reports shell.'
            description='The route is now ready for future participation summaries, CSV export controls, and audit-linked adjustments.'
        />
    )
}
