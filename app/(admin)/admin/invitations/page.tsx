import { EmptyState } from '@/components/ui'

export default function AdminInvitationsPage() {
    return (
        <EmptyState
            eyebrow='Invitations'
            title='Invitation workflows will land here next.'
            description='Local email delivery, secure invite tokens, and acceptance history will sit on top of this reusable empty-state pattern.'
        />
    )
}
